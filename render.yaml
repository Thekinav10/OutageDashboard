services:
  - type: web
    name: outage-watch
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python solution/app.py --month --serve --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/live
    autoDeploy: true
